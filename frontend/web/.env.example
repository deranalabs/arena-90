# Use "live" only after the TxLINE mainnet feed passes end-to-end verification.
NEXT_PUBLIC_TXLINE_MODE=mock

# Optional HTTPS URL for the Arena90 Blink post on X. Leave empty to hide the CTA.
NEXT_PUBLIC_X_BLINK_URL=
